environment:
  matrix:
    - nodejs_version: "7.7"
ervices:
  - mysql
before_install:
  - echo '{"mysql":{"user":"root","password":"Password12!","database":"test","connectionLimit":10}}' > config/default.json
  - mysql -u root -pPassword12! -e 'CREATE DATABASE IF NOT EXISTS test;'
install:
  - ps: Install-Product node $env:nodejs_version
  - npm install
test_script:
  - node --version
  - npm --version
  - npm run lint
  - npm run test:security
  - npm run test:code
build: off